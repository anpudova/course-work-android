package com.feature.materials.impl.mapper

object MaterialMapper {
/*
    fun mapMaterialEntity(material: Material): MaterialEntity {
        with(material) {
            return MaterialEntity(
                id, name, description
            )
        }
    }

    fun mapMaterialModel(material: MaterialEntity): Material {
        with(material) {
            return Material(
                id, name, description
            )
        }
    }
*/
}